/**
 * Intent:
 * This is the development environment configuration file for the MagnetarPrometheus UI.
 * It is used when running the app locally during development. It should point to the
 * local backend API layer (e.g., the local mock adapter or the local Python server).
 */
export const environment = {
  production: false
};
